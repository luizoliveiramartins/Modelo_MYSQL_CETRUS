CREATE DEFINER=`root`@`localhost` TRIGGER `aluno_AFTER_UPDATE` AFTER UPDATE ON `aluno` FOR EACH ROW BEGIN
IF NEW.NOME<>OLD.NOME THEN
INSERT INTO HIST_ALT_ALUNO (CODIGO,DT_ALTERACAO,ALUNO_ANTIGO,SEXO_ANTIGO,EMAIL,OBSERVACAO)
VALUES (NULL,NEW.CODIGO,NOW(),OLD.NOME,OLD.SEXO,OLD.EMAIL,OLD.OBSERVACAO);
END IF;
END